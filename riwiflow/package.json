{
  "name": "riwiflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch src/data/db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
